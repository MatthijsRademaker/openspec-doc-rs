[package]
name = "openspec-doc-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "openspec-doc"
path = "src/main.rs"

[dependencies]
clap.workspace = true
openspec-doc-core.workspace = true
openspec-doc-server.workspace = true
serde_json.workspace = true
# The throwaway project `doctor` probes the hooks against, so no probe can
# resolve to the project being checked.
tempfile.workspace = true
thiserror.workspace = true

