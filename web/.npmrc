# `dist/` is committed and CI fails if a clean rebuild differs from it. Building
# on a Node other than the pinned one is the likeliest way to produce a
# difference that has nothing to do with the source — esbuild and the bundler
# both come from it — so that fails here instead, loudly.
engine-strict=true
