target/
node_modules/
dist/
# web/dist is a build artifact, embedded into the binary at compile time via
# rust-embed. Build it with `make build`; a fresh clone has no dist and cargo
# will not compile without one.
coverage/
web/test-results/
web/playwright-report/
doc_build/
.rspress/
.env
.env.*
!.env.example
.DS_Store
*.log
.pi/openspec-review/

# Local review state: exploration notes, comments, verdicts and directives.
# The repo keeps the approved outcome in openspec/, not the back and forth that
# produced it, so this is machine-local by design.
.openspec-doc/
