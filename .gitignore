target/
node_modules/
dist/
# The dashboard's frontend build is committed, so `cargo install` needs no Node
# toolchain. CI rebuilds it from a clean checkout and fails if it differs — see
# .github/workflows/frontend-assets.yml.
!web/dist/
coverage/
doc_build/
.rspress/
.env
.env.*
!.env.example
.DS_Store
*.log
.pi/openspec-review/
.claude/settings.local.json

# Local review state: exploration notes, comments, verdicts and directives.
# The repo keeps the approved outcome in openspec/, not the back and forth that
# produced it, so this is machine-local by design.
.openspec-doc/
