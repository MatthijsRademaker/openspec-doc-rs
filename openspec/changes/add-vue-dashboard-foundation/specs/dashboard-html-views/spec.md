## ADDED Requirements

### Requirement: The interface offers a light and a dark theme
The system SHALL present the interface in both a light and a dark theme, selectable by the reviewer, and SHALL retain that selection across reloads.

#### Scenario: The chosen theme survives a reload
- **WHEN** a reviewer selects a theme and reloads the page
- **THEN** the system SHALL present the interface in the selected theme
