## 0. Establish the migration boundary

- [x] 0.1 Confirm `add-vue-dashboard-foundation` is complete and the committed `web/dist/` matches its current npm build before changing package managers — verified 2026-08-06: `npm ci && npm run build` on Node 24.15.0/npm 11.12.1 from clean `node_modules`+`dist/` left `git status --porcelain -- web/` empty
- [x] 0.2 Record Bun `1.3.2` as the exact implementation version and fail the change rather than weakening asset freshness if that version cannot reproduce clean build bytes — `bun --version` is 1.3.2; pinned in 1.1, byte reproducibility proven in 1.5
- [x] 0.3 Update `migrate-dashboard-review-to-vue` proposal, design, and prerequisite tasks together so it explicitly waits for this change — completed while creating this proposal so the dependent change does not remain false in the meantime

## 1. Replace npm with one pinned Bun toolchain

- [x] 1.1 Set `packageManager` to `bun@1.3.2`, add `.bun-version`, remove the Node engine, and delete `web/package-lock.json`, `web/.npmrc`, and `web/.nvmrc`
- [x] 1.2 Run `bun install`, commit the resulting text `bun.lock`, then prove `bun install --frozen-lockfile` leaves it unchanged in a clean dependency state — 454 packages from clean `node_modules`; lockfile SHA-256 identical before/after frozen install
- [x] 1.3 Replace every frontend npm command in CI, documentation, and repository scripts with its Bun equivalent; leave no second supported package-manager path — `docs/docs/development/conventions.md` and the CI workflow were the only live npm command sites; `openspec-doc-rs-example/` is a separate fixture project and out of scope
- [x] 1.4 Change `.github/workflows/frontend-assets.yml` to install Bun `1.3.2`, perform a frozen install, run the frontend gate and build, and report all tracked and untracked drift under `web/` — uses `oven-sh/setup-bun@v2` with `bun-version: 1.3.2`; `git status --porcelain` reports untracked drift
- [x] 1.5 Build once after deleting `web/dist/` and once with prior output present, compare complete output bytes, and record the successful reproducibility run in this task — **PASSED** 2026-08-06, Bun 1.3.2: SHA-256 of every file identical across both builds, and identical to the previously committed npm-built assets (`index-9_6NzT1h.js`, `index-In0pt8h5.css`)
- [x] 1.6 Prove the freshness assertion goes red after a controlled source change that leaves `dist/` stale, including newly hashed untracked bundles — **PASSED**: one-character `App.vue` change produced `?? web/dist/assets/index-DZAb36qm.js`, `D index-9_6NzT1h.js`, `M index.html`; reverting and rebuilding restored committed bytes exactly
- [x] 1.7 Repeat the Node- and Bun-free `cargo install --path crates/cli --locked` verification and prove the installed binary serves its shell, bundles, API, and known routes outside the checkout — **PASSED** 2026-08-06: installed to `/tmp/osd-install`, served `/tmp/osd-fixture` with `PATH=/usr/bin:/bin`; `/`, both bundles (byte sizes match), `/api/index`, and `/changes/<name>` all 200, unknown session 404

## 2. Add the bounded frontend quality gate

- [x] 2.1 Validate pinned Biome against every current TypeScript, JSON, CSS, and Vue file; use it only if Vue SFCs remain in scope without broad ignores, otherwise install Vue-aware ESLint plus a formatter and record why — **Biome 2.5.7 adopted**: parses and checks all 39 current TS/JSON/CSS/Vue files (`tailwindDirectives` enabled for the Tailwind v4 syntax); `noUnusedImports`/`noUnusedVariables` are overridden off for `*.vue` only because Biome 2.5 cannot see template usage (52 false positives otherwise) and template-aware `vue-tsc` with `noUnusedLocals` owns that check for SFCs; no file is excluded
- [x] 2.2 Add `format`, `format:check`, `lint`, `typecheck`, `test`, and aggregate `check` scripts, with `check` running formatting, linting, `vue-tsc`, and unit/component tests
- [x] 2.3 Add Vitest, Vue Test Utils, and a DOM environment with configuration that resolves the same aliases and Vue transforms as Vite — vitest 4.1.10 + @vue/test-utils 2.4.11 + jsdom 30.0.1 + @testing-library/vue 8.1.0; `test` block lives in `vite.config.ts`, so plugins and the `@` alias are shared with the build by construction
- [x] 2.4 Add focused unit tests for age formatting, index response handling, and proxy-target resolution, including malformed or failed responses rather than fake success defaults — `age.test.ts` (boundary table, frozen clock), `scopes.test.ts` (503, malformed JSON, network failure), `api-proxy-target.test.ts` (default, override, empty, scheme-less, non-http)
- [x] 2.5 Add component tests for `ScopeTable`, `ThemeToggle`, and the index view's loading, empty, success, and failure states with accessible queries — `getByRole`/`findByRole` throughout; ThemeToggle asserts the persisted `localStorage` value and `dark` class, not just the label
- [x] 2.6 Deliberately introduce one Vue lint error, one type error, and one component regression in turn and confirm each makes `bun run check` fail before reverting them — **PASSED** all three: `debugger` in an SFC script (Biome, exit 1), a type error inside an SFC script (vue-tsc TS2322, proving Volar types SFCs despite the wildcard shim), and a changed empty-state string (2 component tests red)

## 3. Make development and browser routing real

- [x] 3.1 Add Vue Router and move the current index into a route-owned view rendered at `/`, preserving every shipped index field and behavior — vue-router 5.2.0; `App.vue` content moved verbatim to `src/views/IndexView.vue`, `App.vue` is now only `<router-view />`
- [x] 3.2 Register no placeholder session or change routes; add a route-level test proving only implemented views are exposed — `router.test.ts` asserts the route table is exactly `/` and that `/sessions/:id` and `/changes/:name` resolve to nothing
- [x] 3.3 Add a pure Vite API-proxy target resolver with `http://127.0.0.1:8791` as the default and `OPENSPEC_DOC_API_PROXY_TARGET` as the explicit override, with unit tests for both — `src/lib/api-proxy-target.ts`; empty and non-http overrides throw instead of silently defaulting
- [x] 3.4 Proxy `/api` through Vite and verify `openspec-doc serve --port 8791 --no-open` plus `bun run dev` loads real `/api/index` data without CORS configuration — **PASSED** 2026-08-06: fixture server on 8791, `bun run dev` on 5199; `curl localhost:5199/api/index` returned the fixture's real index JSON, shell served, zero `access-control-*` headers

## 4. Establish the review-workbench design source

- [x] 4.1 Create `designs/dashboard-review-workbench.md` defining document-first hierarchy, inline conversation, reviewer/agent distinction, review and delivery states, responsive behavior, accessibility, and restrained motion — reference defines hierarchy, anchored conversation, role labels, semantic state language, WCAG/focus requirements, narrow composition, and reduced-motion policy
- [x] 4.2 Commit desktop and narrow reference frames for the index and intended scope-review shell, showing realistic long identifiers, comments, statuses, and both themes rather than empty placeholders — four offline SVGs under `designs/frames/` (index/scope × desktop/narrow), each rendering light and dark panels with realistic identifiers, comments, verdicts, and delivery states
- [x] 4.3 Bundle the selected local typography assets—IBM Plex Sans and IBM Plex Mono unless implementation finds a concrete defect—and make `components.json`, CSS, and the design reference agree on what renders — exact `@fontsource` packages; Latin/Latin-ext IBM Plex Sans variable plus Latin IBM Plex Mono; `components.json` says `ibm-plex-sans`, CSS uses bundled families, design reference names both; no remote font URL
- [x] 4.4 Add central semantic tokens for open, addressed, resolved, verdict, delivery, reviewer, and agent states in light and dark themes; use no component-local raw status colors — `:root`/`.dark` values and Tailwind `--color-*` aliases in `web/src/style.css`; verdict badge consumes `bg-verdict text-verdict-foreground`
- [x] 4.5 Add an embedded favicon or otherwise eliminate the current unconditional `/favicon.ico` browser-console error — inline SVG data URI in `web/index.html`, copied into the embedded shell with no favicon network request
- [x] 4.6 Inspect the current index in desktop and narrow light/dark views and record where it conforms to or intentionally defers the design reference; this change must not silently become the scope-page rewrite — **PASSED** through Playwright desktop 1280×800 and narrow 390×844 projects, initial light plus persisted dark reload: identifiers, distinct empty/loading/error model, theme control, Router-owned `/`, and no horizontal clipping conform; document blocks, anchored threads, reviewer/agent conversation, delivery controls, and scope routes remain explicitly deferred to `migrate-dashboard-review-to-vue`

## 5. Exercise the embedded application in Playwright

- [x] 5.1 Add pinned Playwright dependency and configuration under `web/`, with trace retention on failure and deterministic desktop and narrow projects — `@playwright/test` 1.62.1; `playwright.config.ts` retains trace/video on failure, fixes locale/timezone/workers, and defines 1280×800 desktop plus 390×844 narrow projects
- [x] 5.2 Add a launcher that builds the frontend and Rust binary, creates deterministic temporary OpenSpec fixture data, starts `openspec-doc` on a fixed test port, and always tears down its processes and files — `e2e/launch.ts` builds Bun+Cargo, writes fixed-mtime fixture files, polls readiness, terminates/escalates server cleanup, and removes temp root in `finally`
- [x] 5.3 Add browser tests for fixture index data, theme persistence across reload, Router navigation, desktop and narrow reachability, and zero unexpected console or failed-asset errors — **PASSED**: `bun run test:e2e` ran 8 tests across both projects; all passed, including console/page/request health and horizontal-overflow assertions
- [x] 5.4 Break one embedded asset reference and confirm browser verification fails before reverting it — **PASSED**: replaced compiled JS reference with `/assets/missing-e2e-bundle.js`, rebuilt Rust shell, browser health assertion observed 404 + `requestfailed` and exited red; trap restored index and `bun run build` restored good assets
- [x] 5.5 Run the embedded-browser lane in CI after the frontend gate and asset freshness check, without substituting a Vite-only test — workflow installs Chromium, then runs `bun run test:e2e`; launcher builds/serves the Rust embedded shell rather than Vite

## 6. Give Pi and Claude repository-specific frontend skills

- [x] 6.1 Add mirrored frontend-design skills constrained by the review-workbench reference, accessibility, offline assets, and restrained motion rather than generic decorative output — identical `.pi/skills/frontend-design/SKILL.md` and `.claude/skills/frontend-design/SKILL.md`
- [x] 6.2 Add mirrored shadcn-vue skills naming `web/components.json`, existing primitives, semantic-token rules, `bunx --bun` commands, explicit Tailwind sources, and generated-file review — identical skill copies name every requested path/constraint
- [x] 6.3 Add mirrored `openspec-doc-dashboard` skills covering Router ownership, API modules, embedded `dist/`, Bun commands, Rust boundary, and exact frontend and browser gates — identical skill copies include fixed proxy and migration scope boundary
- [x] 6.4 Add mirrored browser-verification skills that use the repository Playwright lane for completion and Playwright CLI for interactive inspection without making a global install a hidden requirement — identical skill copies require `bun run test:e2e` and only `bunx --bun` CLI use
- [x] 6.5 Add a harness-resource check that compares every intentionally shared Pi/Claude skill file byte-for-byte and names drift, then wire it into CI — `scripts/check-harness-resources.mjs` compares four manifest paths and emits each drift path; local check passed; CI runs after E2E
- [x] 6.6 Update `AGENTS.md` to route every `web/` task through dashboard and shadcn guidance and every visual change through the design reference and browser verification — frontend routing section added
- [x] 6.7 Start fresh trusted Pi and Claude sessions and confirm each lists and routes to the four frontend skill families before claiming integration works — fresh `pi --no-session --no-lens` and `claude --print --no-session-persistence` both listed `frontend-design`, `shadcn-vue`, `openspec-doc-dashboard`, `browser-verification`

## 7. Add pinned shared shadcn-vue MCP

- [x] 7.1 Add project `.mcp.json` with one shadcn-vue server using `bunx --bun shadcn-vue@2.8.1 mcp --cwd web`; add no browser MCP or Pi-local duplicate — exact shared config added; CLI reports version 2.8.1 and starts the server
- [x] 7.2 In a fresh Pi session, connect the server, verify Bun/Vue/Vite/Tailwind v4 and resolved `web/` aliases directly from `.bun-version`, `package.json`, `vite.config.ts`, and `components.json`, and successfully search registry metadata through MCP — **PASSED** fresh Pi: MCP configured registry `@shadcn`, search `button` returned `button`/`button-group`; direct files confirmed Bun 1.3.2, Vue/Vite, Tailwind v4 plugin, aliases, UI/style paths
- [x] 7.3 In a fresh Claude session from the repository root, perform the equivalent direct configuration and MCP registry-search verification without changing working directory manually — **PASSED** fresh Claude: MCP registry/search and direct config report matched Pi; no directory change or file edits

## 8. Keep documentation and dependent changes truthful

- [x] 8.1 Update README and contributor conventions for Bun install/build/check/dev/E2E commands, committed assets, fixed proxy port, and the fact that `cargo install` still needs no frontend toolchain — README and `docs/docs/development/conventions.md` now document frozen Bun install, `check`, build, E2E, proxy port 8791, committed `dist/`, and Node/Bun-free install
- [x] 8.2 Update the roadmap so the frontend migration order includes `add-dashboard-development-harness` between the Vue foundation and scope-page cutover — roadmap now lists five frontend changes and places harness between foundation and migration
- [x] 8.3 Remove the stale npm package metadata and every claim that this repository has no JavaScript linter, formatter, or frontend tests — `web/.npmrc`, `.nvmrc`, and npm lockfile are deleted; live docs now name Biome, vue-tsc, Vitest, and Playwright; remaining npm text is historical task/design evidence only
- [x] 8.4 Verify the migration change's proposal, design, tasks, and relevant specs remain mutually consistent after adding this dependency; do not alter its shipped-behavior requirements merely to mention tooling — proposal now names harness after foundation; design and prerequisite task 0.6 agree; dashboard specs retain only scope/API behavior and add no tooling dependency

## 9. Full verification

- [x] 9.1 From a clean dependency state, run `bun install --frozen-lockfile`, `bun run check`, `bun run build`, and `bun run test:e2e` — **PASSED** 2026-08-07: clean Bun 1.3.2 install; Biome/Vue typecheck/28 unit-component tests, production build, and 8 embedded Playwright tests across desktop+narrow all passed
- [x] 9.2 Run `cargo fmt --all --check`, `cargo clippy --workspace --all-targets`, and `cargo test --workspace` — macOS local `notify` delivery misses three watcher tests (documented above); Linux container with Rust 1.97.1, rustfmt/clippy, and `openspec` 1.3.1 completed fmt, clippy, 39 CLI tests, 143 core tests, and 46 server tests
- [x] 9.3 Run the harness-resource drift check and verify the pinned shadcn-vue MCP from both supported harnesses — drift check reports four matching files; fresh Pi and Claude sessions connected/search-tested `shadcn-vue@2.8.1` and directly verified frontend config
- [x] 9.4 Rebuild `web/dist/`, confirm `git status --short -- web` is clean, and confirm no npm lock/config/pin survives — final Bun build passed; after commit `30538e2`, `git status --short -- web` is clean; `web/package-lock.json`, `.npmrc`, `.nvmrc`, and npm/Node pins are absent
- [x] 9.5 Run `openspec validate add-dashboard-development-harness --strict` and `openspec validate migrate-dashboard-review-to-vue --strict` — both changes valid
