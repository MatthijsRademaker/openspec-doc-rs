## 1. Make the thing observable before building it

The mock lane cannot originate an artifact change, so the central behaviour in this change has no way to be seen while it is being written. This comes first for that reason, not for tidiness.

- [x] 1.1 Add a mock handler that rewrites a scope's artifact blocks and broadcasts on the SSE channel without recording a comment, reply, status, or verdict. The existing `broadcast(address)` is reusable; what is missing is a caller that is not a reviewer mutation. — `rewriteMockArtifact` keeps block ids so anchored threads stay attached; `broadcast` now sends the Rust server's `{artifactsChanged,reviewStateChanged}` payload instead of a bare `changed` marker, which the client's `parseLiveUpdate` had always rejected
- [x] 1.2 Make the trigger reachable from the browser — a mock-only route the contributor can hit, or a dev-only control. Editing `data.ts` and restarting is not reachable, and a behaviour you have to restart to observe is a behaviour that will be checked once. — `src/mocks/controls.ts` mounts a fixed trigger that rewrites the artifact named in the current `?artifact=` query
- [x] 1.3 Confirm the trigger, its handler, and the fixtures are absent from the production build. `check:assets` already runs on build; verify the mock path is excluded by the mode gate rather than assuming it. — the bundle was already clean but `public/mockServiceWorker.js` was copied into `dist/` and embedded; the worker now lives in `src/mocks/` and is served by an `apply: 'serve'` Vite middleware, and `check:mocks` fails the build on any mock reference in `dist/`
- [x] 1.4 Exercise the existing deferral path with the new trigger: rewrite an artifact while a composer holds unsent text, confirm the update defers, then dismiss and confirm it applies. This path is specified and, as far as the mock lane goes, has never been driven. — driven in the mock lane: banner appears, rewrite withheld, composer text intact, rewrite applied on Cancel

## 2. Reduced motion, inverted

Do this before adding effects, not after. The current block names selectors individually, and every effect in this change would have to be added to that list by hand.

- [x] 2.1 Replace the hand-maintained selector list in the `prefers-reduced-motion: reduce` block with a global suppression of animation and transition, plus a narrow explicit allowlist for anything that must survive. — durations are zeroed globally with `!important` under `*:not([data-motion="required"])`; the opt-out is that attribute, and the allowlist is empty because nothing needs it yet. Decorative *displacement* is not a duration, so it moved to a `--motion-nudge` token that reduced motion sets to `0px` — the two existing hover transforms now derive from it instead of being listed
- [x] 2.2 Confirm the existing five effects still behave under reduced motion after the inversion — the `button, a` transition, the scope-entry lift, the review-block cross-fade, the comment-action reveal, and smooth `scrollIntoView`. The inversion is a refactor of working behaviour and is the one place in this change that can silently break something already correct. — measured in Chromium under both media states: all four transitions 0.16s → 0s, scope-entry hover translate 2px → 0, comment-action resting translate 4px → 0, and `navigationBehavior()` still returns `auto`, asserted by the existing keyboard/reduced-motion e2e test
- [x] 2.3 Assert in e2e that with reduced motion emulated, every effect added by this change is inert. Not that the page renders — that the computed durations are zero and no transform is applied. — `keeps every added effect inert under reduced motion` measures transition and animation durations on all ten new surfaces including pseudo-elements, and asserts `--motion-nudge` is `0px` rather than checking each displacement

## 3. The arrival mark

- [x] 3.1 Signal an arrival from `replaceLiveState` when artifact content is replaced, and from `mutate` when a reviewer's own submission resolves. Do not change scroll preservation, the generation guards, or the dirty-composer deferral; this adds a signal alongside them and touches none of their logic. — one added line in `replaceLiveState`; `mutate`'s operation now returns the thread its submission touched, and status changes and verdicts return nothing
- [x] 3.2 Fire the signal at the moment the content changes, including for a deferred update — which means at the point the deferral resolves, not at the point the event arrived. — the signal sits inside `replaceLiveState`, which is also what `applyPendingArtifactUpdate` calls, so the deferred case needs no second call site
- [x] 3.3 Render the mark at the document stage, using `--delivery`. Do not promote `.scope-artifact-update` into an always-on banner: it sits at the top of the page and the reviewer is generally not at the top of the page. — a `--delivery` overlay border on `.artifact-document` (an inset shadow is painted over by the header's own background) plus a text report in the header; the stage is taller than the viewport, so its vertical edges are legible from any scroll position
- [x] 3.4 Render the reviewer's own submission mark at the thread it created or extended. — `.artifact-conversation__thread--recorded` in the rail and `.comment-thread--recorded` for a loose thread in the drawer
- [x] 3.5 The mark fades in, holds, and fades out with no residual state, no dismiss control, and no interception of pointer or keyboard interaction. Hold long enough to be caught by someone looking at the other pane. — a Vue `<Transition>` owns the fade and a single 2200ms timer owns the dwell, so the duration is stated once; the overlay is `pointer-events: none` and the report reserves its line so nothing moves
- [x] 3.6 Do not assert which blocks changed anywhere in this work — not in the mark, not in a count, not in a tooltip. Block identity across a rewrite belongs to the anchor resolver and a second answer to it would contradict the first. — the report reads "Document content replaced" and names nothing
- [x] 3.7 Tests: an arrival during an idle page marks and preserves scroll; a deferred arrival marks when it applies rather than when it was detected; a reviewer's own comment marks at its thread; the mark clears on its own; nothing is marked when no update arrived. — five component tests plus an e2e that drives a real file rewrite against the Rust server and asserts the reading position is unmoved to the pixel

## 4. The two-pane lock

- [x] 4.1 First, with no motion: make `.artifact-conversation__thread--active` clearly distinct from an idle thread. An idle thread already carries an accent top border, so the current active treatment is a small delta on a surface the reviewer is being directed to. This is a conformance fix to *Active conversation locks its block visibly* and stands on its own. — every edge turns `--focus-ink` rather than only the left one, the gradient doubles in strength, and the thread grows the same corner ticks the document block has, so the two panes are bracketed identically
- [x] 4.2 Then the lock: the conversation crosshair's two hairlines converge to their intersection on activation, and the active block's existing `::before` / `::after` corner ticks grow from zero width rather than appearing at full width. Same duration, same easing, both panes. — the ticks are now always present at `scaleX(0)` so they have something to grow from; the crosshair's hairlines scale from their intersection outward, both on `--motion-duration` / `--motion-easing`
- [x] 4.3 Drive it from both directions — marker click and thread click — and confirm the gesture reads the same either way. Activation is symmetric today and the motion must not make it feel directional. — both directions write the same `activeThreadId`, so nothing in the treatment can be directional; asserted in e2e from each side
- [x] 4.4 Look at it for a while, on real content, at 1280 and wider. If it reads as a flourish rather than as an instrument, delete it and keep 4.1. That fallback meets the requirement on its own and taking it is not a failure of the change. — **kept.** Inspected at 1280 and 1536 against the mock lane's realistic content. It reads as an instrument: 1.5rem, on-idiom (the crosshair is already the pane's empty-state motif), visible only while a thread is locked, and it answers "which thread is bound to which block" at the far side of the screen from the click. If a later reviewer disagrees, deleting the `__crosshair-lock` rules and its span leaves 4.1 intact
- [x] 4.5 Assertions: activating a marker changes the conversation pane's rendered state, and activating a thread changes the document pane's — verified against the pane the reviewer did not click, which is the case that fails today. — a component test per direction plus `locks both panes from either direction of activation` in e2e, which reads the grown tick's computed transform rather than just the class

## 5. Transitions on the three surfaces

Sequenced last deliberately: cheapest work here, and `fix-workbench-obstructions` moves two of these three.

- [x] 5.1 Drawer and backdrop: fade plus a slight rise, within the 100–200ms envelope. Do this after the trigger/drawer split from `fix-workbench-obstructions` has landed. — two `<Transition>`s inside the existing `Teleport`; the rise is `4 * --motion-nudge` written into a full `translate()` so it composes with the drawer's centring translate. The enter/leave rules had to be placed *after* the drawer's own rule — at equal specificity an earlier `transform` loses, and the rise silently never happens (it did, and was caught by measuring mid-transition)
- [x] 5.2 Inline composer: expand so the document's displacement reads as the composer opening. Confirm it does not fight the block's existing border cross-fade. — a `grid-template-rows: 0fr → 1fr` slot around the form; the block's own transition is border and background only, so there is nothing to fight
- [x] 5.3 Conversation collapse: `.scope-layout`'s `grid-template-columns` uses `clamp()` and `minmax()`, which do not interpolate reliably. Transition between fixed lengths at the endpoints or use a different mechanism — do not discover this halfway and reach for a JavaScript height animation. — the collapsed rail is written `minmax(3.25rem, 3.25rem)` instead of `3.25rem`, so both track lists have the same shape and the property interpolates. Measured mid-transition: 416px → 86px → 52px. This keeps every expanded width byte-identical, which transitioning between fixed lengths would not have
- [x] 5.4 Confirm the collapse transition composes with the capped document measure from `fix-workbench-obstructions`. With the measure fixed the document should slide rather than reflow; if it still reflows, that change did not land as intended and this is where it shows. — the document column goes 598px → 962px while the 48ch measure stays below both, so no line re-wraps; the existing `keeps document line breaks identical across conversation rail collapse` test asserts it

## 6. Cleanup and close out

- [x] 6.1 Delete `tw-animate-css` from `web/src/style.css` and from `package.json`, and refresh the lockfile with `bun install`. It has never been used; leaving it beside hand-written keyframes invites a second motion vocabulary. — removed; the lockfile diff is the three lines for that one package. Note the install ran on Bun 1.3.4, not the pinned 1.3.2, so the lockfile should be re-checked on the pinned version before this lands
- [x] 6.2 Update `openspec/specs/dashboard-visual-system/spec.md`, `openspec/specs/dashboard-html-views/spec.md`, and the `dashboard-development-harness` spec with this change's deltas. — all three synced. The harness one was blocked until `add-dashboard-development-harness` was archived, which the reviewer directed be done first: that change's own delta created `openspec/specs/dashboard-development-harness/spec.md` with its eight requirements, and *The mock lane can originate an artifact change* was appended as the ninth. `openspec validate --specs` passes on every capability
- [x] 6.3a Not in the original list, but Rule 12 requires it: `README.md`'s mock-lane paragraph named only handlers and fixtures under `web/src/mocks/`, and `docs/docs/development/conventions.md` named only `check:assets` as a build gate. Both now describe the worker script's move, the rewrite trigger, `check:mocks`, and the motion rule this change changes.
- [x] 6.3 Update `designs/dashboard-review-workbench.md`. Its motion paragraph forbids animated ornament and bounds transitions at 100–200ms without distinguishing a transition's duration from a notice's dwell; as written it reads as forbidding the arrival mark. — the paragraph now separates a transition's completion time from a notice's dwell and states that reduced-motion suppression is global with an explicit opt-out
- [x] 6.4 Run `bun run check` and `bun run test:e2e`. — `check` clean with no lint warnings; 29 passed / 15 project-skipped across desktop and narrow
- [x] 6.5 Rebuild `web/dist/` before Cargo. — rebuilt, then `cargo build --locked --package openspec-doc-cli` succeeded and the live verification below ran against that binary

## 7. Live verification

- [x] 7.1 With `openspec-doc serve` running against this repository, have an agent edit an artifact of an open change scope while the page is open and scrolled into the document. Confirm the mark appears at the document, the reading position is unmoved, and the mark clears itself. — served this repository on 8795 and rewrote this change's own `design.md` with the page scrolled 900px into it: the report appeared, `window.scrollY` stayed at 900, the rewritten prose rendered, and the report detached on its own. The artifact was restored byte-exactly afterwards
- [x] 7.2 Repeat with a composer holding unsent text. Confirm the deferral still holds, and that the mark appears when the update applies rather than when it was detected. — the deferral banner appeared, the report did not, the content did not change, and both arrived together on Cancel. A first pass tripped the "reported before it applied" guard because the probe's own restore-write had legitimately marked the document seconds earlier — two arrivals in quick succession share one dwell, which is the intended behaviour of resetting the timer
- [x] 7.3 Repeat 7.1 with the OS reduced-motion setting on. Confirm the content still updates and the arrival is still discernible without motion. — same result with `reducedMotion: reduce`. Worth recording *why* it stays discernible from a mid-document scroll position: the header's text report is off-screen there, and what carries the arrival is the stage's two vertical `--delivery` edges, which is the reason the mark is at the stage rather than in a banner
